# DEPRECATED ALIAS — DO NOT EDIT
# ================================
# This file was renamed to lifecycle.yaml (it declares capability LIFECYCLE,
# not capability routing).  The routing contract lives at ../capabilities.yaml.
#
# This alias exists only so older scripts/docs that reference the old path
# fail loudly with a pointer instead of silently reading a stale file.
# It will be deleted once no references remain (grep: skills/capabilities.yaml).
#
# See:
#   skills/lifecycle.yaml   — lifecycle declarations (status / action)
#   ../capabilities.yaml    — routing contract (triggers / bindings)
