// COPYRIGHT DASSAULT SYSTEMES 2026
// Dialog strings for <DialogClassName> (English / default)
// 合并到 framework 共享 catalog: <FrameworkName>.CATNls
//
// 与 DialogClass.cpp 中的 NLS(key, fallback) 调用一一对应；
// key 用语义名（类名前缀），fallback 已在代码里保底。

// 对话框窗口标题
<DialogClassName>.Title = "<DialogClassName>";

// 控件文本
<DialogClassName>.LabelId = "Value:";
